[project]
name = "bugskills"
version = "0.1.0"
description = "Convert bug bounty reports into AI skills"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[project.scripts]
bugskills = "main:main"
